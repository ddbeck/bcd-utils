{
  "name": "@ddbeck/bcd-utils",
  "version": "0.2.0",
  "description": "Utilities for working with @mdn/browser-compat-data.",
  "type": "module",
  "exports": "./src/index.js",
  "scripts": {
    "test": "mocha 'src/**.test.js' && npm run format:check",
    "format:check": "prettier --check .",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "eslint": "^8.36.0",
    "mocha": "^10.2.0",
    "prettier": "^2.8.6"
  },
  "dependencies": {
    "compare-versions": "^5.0.3"
  },
  "peerDependencies": {
    "@mdn/browser-compat-data": ">=5.2.0"
  }
}
